import ListView from "../pages/employees/list";
import "@testing-library/jest-dom";
import { fireEvent, render, screen } from "@testing-library/react";

describe('ListView', () => {
    it('renders the List view properly', () => {
      render(<ListView />)
  
      
    })
  })